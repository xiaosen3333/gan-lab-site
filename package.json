{
  "name": "gan-lab-site",
  "private": true,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/build-site.mjs --check && node scripts/check-site.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/serve.mjs"
  },
  "engines": { "node": ">=20" }
}
